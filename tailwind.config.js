const colors = require("tailwindcss/colors");

module.exports = {
    purge: [],
    darkMode: false, // or 'media' or 'class'
    theme: {
        extend: {},
        container: {
            center: true,
            padding: "2rem",
        },
        fontFamily: {
            sans: "Patrick Hand",
        },
    },
    variants: {
        extend: {},
    },
    plugins: [],
};
